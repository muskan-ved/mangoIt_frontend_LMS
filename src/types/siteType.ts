export type siteType = {
    org_title?: string,
    org_logo: string,
    org_favicon: string,
    user_id?: string
  };

  export type stripeType = {
    org_pk: string,
    org_sk: string,
    user_id?: string
  };

