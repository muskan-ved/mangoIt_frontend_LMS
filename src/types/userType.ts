export type userType = {
    first_name: string,
    last_name: string,
    email: string,
    password?: string,
    confirm_password?: string,
    profile?: string,
    role:number,
    profile_pic:string
}